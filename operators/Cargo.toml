[package]
name = "operators"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
regex = "1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
kubewarden-policy-sdk = "0.14.0"

[dev-dependencies]
rstest = "0.26.0"
serde_yaml = "0.9"
